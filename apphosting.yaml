runConfig:
  cpu: 1
  memory: 512Mi
  minInstances: 0
  maxInstances: 10
  env:
    - variable: FLASK_ENV
      value: production
    - variable: PYTHONPATH
      value: /workspace

entrypoint: gunicorn --bind :$PORT --workers 1 --threads 8 --timeout 0 main:app

buildConfig:
  runtime: python311
