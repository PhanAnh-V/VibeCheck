runConfig:
  cpu: 1
  memory: 512Mi
  minInstances: 0
  maxInstances: 10
  env:
    - variable: FLASK_ENV
      value: production
    - variable: PYTHONPATH
      value: /workspace

entrypoint: gunicorn -w 2 -k uvicorn.workers.UvicornWorker main:app

buildConfig:
  runtime: python311
